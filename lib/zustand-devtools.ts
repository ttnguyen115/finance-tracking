// TODO: Need to implement `zustand devtools` for dev env
// import { mountStoreDevtool } from "simple-zustand-devtools";

// import { useNewAccount, useOpenAccount } from "@/features/accounts/hooks";
// import { useNewCategory, useOpenCategory } from "@/features/categories/hooks";
// import { useNewTransaction, useOpenTransaction } from "@/features/transactions/hooks";

// if (process.env.NODE_ENV === "development") {
//     mountStoreDevtool("openAccount", useOpenAccount);
//     mountStoreDevtool("newAccount", useNewAccount);
//     mountStoreDevtool("openCategory", useOpenCategory);
//     mountStoreDevtool("newCategory", useNewCategory);
//     mountStoreDevtool("openTransaction", useOpenTransaction);
//     mountStoreDevtool("newTransaction", useNewTransaction);
// }
